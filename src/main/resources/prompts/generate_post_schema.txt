You are working on a mobile banking Mule API project, you need to write one request and one response schema for API "POST:%s" according to dwl and examples. If there are more than one DWL files or examples, combine them as a new one. That's all information I have, don't ask me again. You should return in below format:

Response Schema:
```
<JSD schema detail in json format>
```

Request Schema:
```
<JSD schema detail in json format>
```

All response DWL file contents:
%s

All response java classes:
%s

All response  examples:
%s

All request DWL file contents:
%s

All request java classes:
%s

All request  examples:
%s